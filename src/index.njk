---
layout: base.njk
---

<ul>
  {%- for post in collections.posts | reverse -%}
  <li>
    <a href="{{ post.url }}">{{ post.data.title }}</a>
    Updated <time datetime="{{ post.date | dateIso }}">{{ post.date | dateReadable }}</time>
  </li>
  {%- endfor -%}
</ul>
